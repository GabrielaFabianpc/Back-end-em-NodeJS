const usuarios = ["Joana", "renata", "cleber", "zeca"];

let indiceDoUsuario = 0;

while (indiceDoUsuario < usuarios.length) {
  console.log(usuarios[indiceDoUsuario]);
  indiceDoUsuario += 1;
}

// usuarios.forEach((element) => {
//   console.log(element);
// });
