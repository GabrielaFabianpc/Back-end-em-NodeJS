const numero = 20;
const numeroDois = 50;

function somar(num1, num2) {
  return num1 + num2;
}
console.log(somar(numero, numeroDois));

const somando = (numberOne, numberTwo) => {
  return numberOne + numberTwo;
};
console.log(somando(numero, numeroDois));

const nome = "Gabriela";

console.log(`meu nome é ${nome}`);
console.log(`Ola ${nome}, sue numero favorito é: ${numero}`);

const conta = [2, 3, 4];
console.log(conta);
